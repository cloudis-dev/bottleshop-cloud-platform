{
  "name": "@bottleshop/api",
  "description": "Firebase Functions package, auto generated by @simondotm/nx-firebase",
  "scripts": {
    "lint": "cd ../../ && nx lint bottleshop-api",
    "build": "cd ../../ && nx build bottleshop-api --with-deps",
    "serve": "npm run build && firebase emulators:start --only functions --config ../../firebase.bottleshop-api.json",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions --config ../../firebase.bottleshop-api.json",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "16"
  },
  "main": "src/index.js",
  "dependencies": {},
  "devDependencies": {},
  "private": true
}
